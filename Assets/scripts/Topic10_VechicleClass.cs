using UnityEngine;

public class Topic10_VechicleClass : MonoBehaviour
{
    
    public string parentMessage = "Hello from Parent!";

    public int vechicleCost = 100;
    void Start()
    {
        Debug.Log(parentMessage);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
